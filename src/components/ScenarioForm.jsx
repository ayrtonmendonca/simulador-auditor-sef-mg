import React from 'react';

export default function ScenarioForm({ scenarios, setScenarios }) {
  return (
    <div className="bg-white p-4 rounded shadow mb-4">
      <h2 className="text-lg font-semibold mb-2">Adicionar Cenário</h2>
      <p>Formulário para incluir cenários (implemente os campos necessários)</p>
    </div>
  );
}